<div class="row section_16_bg overflow-hidden {{ data.class }}" style="{{ data.style }}" id="section_16">
    <div class="section_16 col">
        <div class="container cont-site">
            <h3 class="section_16__header text_bicolor">{{ data.header }}</h3>
            {% for block in data.blocks %}
                {% include block.block with {data: block} %} 
            {% endfor %}
        </div>
    </div>
</div>